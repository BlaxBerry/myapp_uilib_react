import { Meta, Title, Subtitle, Controls, Story } from "@storybook/blocks";
import * as Stories from "./index.stories";

<Meta of={Stories} />

<Title>\<ThemeProvider></Title>

<Subtitle>应用的主题 Provider</Subtitle>

使用时需作为父组件包裹本UI包中的所有组件来实现主题的自定义

```tsx
import { ThemeProvider, type ThemeProviderProps } from "myapp_uilib_react";
```

```tsx
import {
  samplePaletteNames,
  samplePalettes,
  type SamplePaletteName,
} from "myapp_uilib_react";
```

<Controls />

## 基本使用

<Story of={Stories.BaseExample} />

```tsx
import React from "react";
import {
  ThemeProvider,
  samplePaletteNames,
  getSamplePaletteColor,
  type SamplePaletteName,
} from "myapp_uilib_react";

// samplePaletteNames[3]; // "TEAL"
// getSamplePaletteColor("TEAL", "primary", "main"); // '#26a69a'
// getSamplePaletteColor("CRIMSON", "secondary", "dark"); // '#e91e63'

const paletteName: SamplePaletteName = "TEAL"; // 鸭羽青色

function App() {
  return (
    <ThemeProvider isDark={false} themePaletteName={paletteName}>
      // 其他组件
    </ThemeProvider>
  );
}
```
